DELETE FROM MOM_USER_TASK 
WHERE rowid not in(SELECT MIN(rowid)
FROM MOM_USER_TASK
GROUP BY USERID, TASK, MOMDATE,TASKTIME)
/
Alter table MOM_USER_TASK ADD CONSTRAINT "MOM_UNIQUE" UNIQUE ("USERID", "TASK", "MOMDATE", "TASKTIME") ENABLE

